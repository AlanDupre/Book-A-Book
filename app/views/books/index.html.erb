<h1>Liste de livres</h1>
<ul>
  <% @books.each do |book| %>
    <li><%= book.title %></li>
    <li><%= book.description %></li>
    <li><%= book.author %></li>
    <li><%= book.isbn %></li>
  <% end %>
</ul>